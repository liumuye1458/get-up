console.error('⚠️ Electron 为历史参考路径，不允许用于当前产品开发')
console.error('Legacy Electron / Web runtime is archived and blocked.')
console.error('Current product is PURE WinUI native desktop.')
process.exit(1)
